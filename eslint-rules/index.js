import noConsoleOutsideDevelopmentMode from './no-console-outside-development-mode.js';

const rules = {
	rules: {
		'no-console-outside-dev-mode': noConsoleOutsideDevelopmentMode,
		// Добавьте здесь другие правила
	},
};

export default rules;
